// Code generated by github.com/smartcontractkit/chainlink-common/pkg/capabilities/protoc, DO NOT EDIT.

package server

import (
	"context"
	"fmt"
	"time"

	"github.com/smartcontractkit/chainlink-common/pkg/capabilities/stubs/don/cron"
	"github.com/smartcontractkit/chainlink-common/pkg/loop"

	"github.com/smartcontractkit/chainlink-common/pkg/capabilities"
	"github.com/smartcontractkit/chainlink-common/pkg/types/core"
)

type CronCapability interface {
	RegisterTrigger(ctx context.Context, metadata capabilities.RequestMetadata, input *cron.Config) (<-chan capabilities.TriggerAndId[*cron.CronTrigger], error)
	UnregisterTrigger(ctx context.Context, metadata capabilities.RequestMetadata, input *cron.Config) error
	Start(ctx context.Context) error
	Close() error
	HealthReport() map[string]error
	Name() string
	Description() string
	Ready() error
	Initialise(ctx context.Context, config string, telemetryService core.TelemetryService, store core.KeyValueStore, errorLog core.ErrorLog, pipelineRunner core.PipelineRunnerService, relayerSet core.RelayerSet, oracleFactory core.OracleFactory) error
}

func NewCronServer(capability CronCapability) loop.StandardCapabilities {
	return &cronServer{
		cronCapability: cronCapability{CronCapability: capability},
	}
}

type cronServer struct {
	cronCapability
	capabilityRegistry core.CapabilitiesRegistry
}

func (cs *cronServer) Initialise(ctx context.Context, config string, telemetryService core.TelemetryService, store core.KeyValueStore, capabilityRegistry core.CapabilitiesRegistry, errorLog core.ErrorLog, pipelineRunner core.PipelineRunnerService, relayerSet core.RelayerSet, oracleFactory core.OracleFactory) error {
	if err := cs.CronCapability.Initialise(ctx, config, telemetryService, store, errorLog, pipelineRunner, relayerSet, oracleFactory); err != nil {
		return fmt.Errorf("error when initializing capability: %w", err)
	}

	cs.capabilityRegistry = capabilityRegistry

	if err := capabilityRegistry.Add(ctx, &cronCapability{
		CronCapability: cs.CronCapability,
	}); err != nil {
		return fmt.Errorf("error when adding kv store action to the registry: %w", err)
	}

	return nil
}

func (cs *cronServer) Close() error {
	ctx, cancel := context.WithTimeout(context.Background(), time.Second)
	defer cancel()
	if err := cs.capabilityRegistry.Remove(ctx, "cron@1.0.0"); err != nil {
		return err
	}

	return cs.cronCapability.Close()
}

func (cs *cronServer) Infos(ctx context.Context) ([]capabilities.CapabilityInfo, error) {
	// TODO if we get rid of targets in favour of actions that return empty proto, do we need Consensus stil?
	info, err := cs.cronCapability.Info(ctx)
	if err != nil {
		return nil, err
	}
	return []capabilities.CapabilityInfo{info}, nil
}

type cronCapability struct {
	CronCapability
}

func (c *cronCapability) Info(ctx context.Context) (capabilities.CapabilityInfo, error) {
	// TODO this is problematic right not because we can do both...?
	// Maybe we do need to split it out, even if the user doesn't see it
	return capabilities.NewCapabilityInfo("cron@1.0.0", capabilities.CapabilityTypeAction, c.CronCapability.Description())
}

var _ capabilities.TriggerCapability = (*cronCapability)(nil)

func (c *cronCapability) RegisterTrigger(ctx context.Context, request capabilities.TriggerRegistrationRequest) (<-chan capabilities.TriggerResponse, error) {
	switch request.Method {
	case "Trigger":
		input := &cron.Config{}
		return capabilities.RegisterTrigger(ctx, "cron@1.0.0", request, input, c.CronCapability.RegisterTrigger)
	default:
		return nil, fmt.Errorf("method %s not found", request.Method)
	}

}

func (c *cronCapability) UnregisterTrigger(ctx context.Context, request capabilities.TriggerRegistrationRequest) error {
	switch request.Method {
	case "Trigger":
		input := &cron.Config{}
		_, err := capabilities.FromValueOrAny(request.Config, request.Request, input)
		if err != nil {
			return err
		}
		return c.CronCapability.UnregisterTrigger(ctx, request.Metadata, input)
	default:
		return fmt.Errorf("method %s not found", request.Method)
	}
}
