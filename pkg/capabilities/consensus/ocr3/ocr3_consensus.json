{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/smartcontractkit/chainlink/capabilities/ocr3_consensus@1.0.0",
  "properties": {
    "config": {
      "properties": {
        "aggregationMethod": {
          "type": "string",
          "enum": ["data_feeds"]
        },
        "aggregationConfig": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "feedId": {
                "type": "string",
                "pattern": "^0x[0-9a-f]{64}$",
                "description": "The ID of the data feed.",
                "examples": [
                  "0x0003fbba4fce42f65d6032b18aee53efdf526cc734ad296cb57565979d883bdd"
                ]
              },
              "deviation": {
                "type": "number",
                "maximum": 1,
                "description": "The deviation that is required to generate a new report. Expressed as a percentage. For example, 0.01 is 1% deviation."
              },
              "heartbeat": {
                "type": "integer",
                "minimum": 1,
                "description": "The interval in seconds after which a new report is generated, regardless of whether any deviations have occurred. New reports reset the timer."
              }
            },
            "additionalProperties": false,
            "minItems": 1,
            "required": ["feedId", "deviation", "heartbeat"]
          }
        },
        "encoder": {
          "type": "string",
          "enum": ["EVM"]
        },
        "encoderConfig": {
          "type": "object",
          "properties": {
            "abi": {
              "type": "string",
              "description": "The ABI for report encoding.",
              "examples": [
                "(bytes32 FeedID, uint224 Price, uint32 Timestamp)[] Reports"
              ]
            }
          },
          "additionalProperties": false,
          "required": ["abi"]
        },
        "reportId": {
          "type": "string",
          "pattern": "^[a-f0-9]{4}$",
          "examples": ["0x0001"]
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "aggregationMethod",
        "aggregationConfig",
        "encoder",
        "encoderConfig",
        "reportId"
      ]
    },
    "inputs": {
      "properties": {
        "observations": {
          "properties": {
            "Underlying": {
              "items": true,
              "type": "array"
            }
          },
          "additionalProperties": false,
          "type": "object",
          "required": ["Underlying"]
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": ["observations"]
    },
    "outputs": {
      "properties": {
        "WorkflowExecutionID": {
          "type": "string"
        },
        "Value": {
          "properties": {
            "Underlying": {
              "type": "object"
            }
          },
          "additionalProperties": false,
          "type": "object",
          "required": ["Underlying"]
        },
        "Err": true
      },
      "additionalProperties": false,
      "type": "object",
      "required": ["WorkflowExecutionID", "Value", "Err"]
    }
  },
  "additionalProperties": false,
  "type": "object",
  "required": ["config", "inputs", "outputs"],
  "description": "OCR3 consensus exposed as a capability."
}
